const express = require('express');
const router = express.Router();
// const controller = require('../controllers/collaboration.controller');

// Add routes for any HTTP-based collaboration features

module.exports = router;
