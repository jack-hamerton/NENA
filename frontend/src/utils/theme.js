import { xiaoGenshin } from '../theme/xiaoGenshin';

export const getTheme = () => {
  // For now, we'll just return the default theme.
  // In the future, we could add logic to switch between themes.
  return xiaoGenshin;
};
