import React from 'react';

const TestPage = () => {
  return (
    <div style={{ padding: '20px', color: 'white', backgroundColor: '#35424c', minHeight: '100vh' }}>
      <h1>App is Loading!</h1>
      <p>If you see this, the app is working.</p>
      <p>If you still see blank space, there's a JavaScript error.</p>
      <p>Open browser dev tools (F12) to check the console.</p>
    </div>
  );
};

export default TestPage;
